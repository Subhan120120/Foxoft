---
trigger: always_on
---

Prefer DevExpress UI components when designing Windows Forms interfaces.

When creating or modifying forms, use DevExpress controls instead of standard WinForms controls where appropriate. Keep the UI modern, consistent, and compatible with the existing DevExpress-based project structure.